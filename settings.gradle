pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = 'scraper-backend'

include 'api-service', 'publisher-service', 'worker-service'